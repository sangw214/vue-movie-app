[build]
  command = "npm run build"
  functions = "functions"
  publish = "dist"

[dev]
  frameworkd = "#custom"
  command = "npm run dev:webpack"
  targetPort = 8079
  port = 8080
  publish = "dist"
  autoLaunch = false